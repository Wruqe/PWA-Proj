{
  "name": "mini-project-main",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:dev": "concurrently \"cd server && pnpm run server\" \"cd client && pnpm run dev\"",
    "start": "pnpm run build && cd server && node server.js",
    "server": "cd server nodemon server.js --ignore client",
    "build": "cd client && pnpm run build",
    "install": "cd server && pnpm i && cd ../client && pnpm i",
    "client": "cd client && pnpm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
